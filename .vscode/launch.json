{
  "configurations": [
    {
      "type": "cppdbg",
      "request": "launch",
      "name": "Launch",
      "program": "${workspaceFolder}/build/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "CMake: build"
    },
  ]
}